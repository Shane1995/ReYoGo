{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "packages/.*\\.(ts|tsx|js|cjs|mjs)$",
        "cmd": "cd ${workspaceFolder} && ./node_modules/.bin/oxfmt ${file}"
      }
    ]
  }
}
