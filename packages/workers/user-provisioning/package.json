{
  "name": "@reyogo/worker-user-provisioning",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
